import React, { Component } from 'react';
import { connect } from 'react-redux';

class SearchResultDetailPanel extends Component {

  render() {
    return (
      <div>
      </div>
    );
  }

}

function mapStateToProps(state, ownProps) {
  return {};
}

export default connect(mapStateToProps)(SearchResultDetailPanel);
